# GAN 接入代理模型标签损失技术路线

## 已确认目标

1. 在现有单阶段 CWGAN-GP 训练基础上，为生成器新增辅助损失项。
2. 辅助损失目标是让生成翼型的气动标签误差更小。
3. 气动代理模型只约束 `CL` 和 `CM`，不约束 `CD`。
4. 同时加入可微厚度约束，使生成翼型厚度接近条件标签中的 `Thickness`。
5. 不修改判别器损失和梯度惩罚定义。
6. 统一项目中的翼型厚度定义，数据集标签、评估脚本和 GAN 厚度损失使用同一套计算逻辑。

## 标签与模型接口

GAN 条件标签顺序固定为：

`[alpha, Re, CL, Thickness, CM]`

代理模型输入为：

`normalized_coords + normalized_[alpha, Re]`

代理模型输出顺序固定为：

`[CM, CL, CD]`

因此生成器辅助气动损失只取代理输出中的 `CM` 和 `CL`，分别对齐 GAN 条件标签中的 `CM` 和 `CL`。

## 生成器损失

原始生成器损失为：

`L_adv = -mean(D(G(z, cond), cond))`

新增后生成器损失使用动态权重：

`L_G = w_adv(epoch) * L_adv + w_surrogate(epoch) * L_surrogate + w_thickness(epoch) * L_thickness`

其中：

`L_surrogate = MSE(predicted_[CM, CL], target_[CM, CL])`

`L_thickness = MSE(predicted_thickness_norm, target_thickness_norm)`

### 动态权重阶段

配置项：

- `gan_aux_start_epoch: 120`
- `gan_aux_ramp_epochs: 60`
- `gan_adv_loss_final_weight: 0.1`
- `gan_surrogate_loss_weight: 0.1`
- `gan_thickness_loss_weight: 0.1`

阶段定义：

1. `epoch < gan_aux_start_epoch`：纯 GAN 阶段，不加载也不调用代理模型。
   - `w_adv = 1`
   - `w_surrogate = 0`
   - `w_thickness = 0`
2. `gan_aux_start_epoch <= epoch < gan_aux_start_epoch + gan_aux_ramp_epochs`：线性切换阶段。
   - `progress = (epoch - gan_aux_start_epoch + 1) / gan_aux_ramp_epochs`
   - `w_adv = 1 + progress * (gan_adv_loss_final_weight - 1)`
   - `w_surrogate = progress * gan_surrogate_loss_weight`
   - `w_thickness = progress * gan_thickness_loss_weight`
3. `epoch >= gan_aux_start_epoch + gan_aux_ramp_epochs`：标签误差主导阶段，保留最终对抗约束。
   - `w_adv = gan_adv_loss_final_weight`
   - `w_surrogate = gan_surrogate_loss_weight`
   - `w_thickness = gan_thickness_loss_weight`

## 厚度损失定义

项目统一厚度定义为：

`thickness = (max(y) - min(y)) / (max(x) - min(x) + 1e-8)`

`utils.calculate_relative_thickness()` 使用该定义的 NumPy 版本，`utils.calculate_relative_thickness_torch()` 使用同一定义的 PyTorch 版本。数据集生成、GAN 评估和 GAN 训练厚度损失必须调用 `utils.py` 中的统一实现，不允许在训练脚本里重复定义厚度公式。

该定义不是插值上下表面后的传统最大厚度定义。它的优点是语义简单、稳定、可微，并且可以在数据标签和训练损失之间保持一致。

为了和 GAN 条件标签空间一致，厚度损失在归一化标签空间计算：

`predicted_thickness_norm = (predicted_thickness - cond_mean[3]) / cond_std[3]`

`target_thickness_norm = cond[:, 3]`

## 归一化与检查点

1. 代理模型权重从 `config['surrogate_best_model_path']` 加载。
2. 代理模型归一化参数从 `config['surrogate_norm_path']` 加载。
3. GAN 标签归一化参数从 `model/cond_norm.pt` 加载。
4. 坐标反归一化参数从 `model/coord_norm.pt` 加载。
5. 代理模型在 GAN 训练期间冻结参数，只允许梯度穿过代理模型回传到生成器输出坐标。
6. 如果启用代理损失但缺少代理模型或归一化文件，应 fail-fast 报错。

## 配置项

新增配置项：

- `gan_aux_start_epoch: 120`
- `gan_aux_ramp_epochs: 60`
- `gan_adv_loss_final_weight: 0.1`
- `gan_surrogate_loss_weight: 0.1`
- `gan_thickness_loss_weight: 0.1`

这些权重只影响生成器损失，不影响判别器训练。纯 GAN 阶段不调用代理模型，避免代理模型前向带来的额外开销。

## 执行步骤

1. 修改 `config.yaml` 增加两个 GAN 辅助损失权重。
2. 修改 `train.py`：
   - 加载并冻结 `AerodynamicSurrogate`
   - 加载代理归一化参数、GAN 条件归一化参数、坐标归一化参数
   - 新增可微坐标反归一化函数
   - 在生成器更新时计算 `L_surrogate` 与 `L_thickness`
   - 记录并打印辅助损失均值
3. 修改 `train.py` 增加动态权重函数，并在纯 GAN 阶段跳过代理模型加载和调用。
4. 修改 `utils.py`，统一 NumPy 与 PyTorch 厚度计算实现。
5. 确认 `prepare_dataset.py`、`eval_cgan.py`、`test_cgan.py` 通过 `utils.calculate_relative_thickness()` 使用统一定义。
6. 运行语法检查。
7. 运行最小训练冒烟测试，确认前向、反向和保存流程可执行。

## GAN 指标记录与绘图

训练代码不直接持有绘图逻辑。训练阶段每个 epoch 保存一行 CSV 指标到：

`model/gan_training_metrics.csv`

CSV 字段为：

`epoch,d_loss,g_loss_total,g_adv_raw,surrogate_raw,thickness_raw,g_adv_weighted,surrogate_weighted,thickness_weighted,real_score,fake_score,grad_norm,w_adv,w_surrogate,w_thickness`

新增独立脚本 `plot_gan_metrics.py`，只负责读取 CSV 并生成曲线图：

- 输入：`model/gan_training_metrics.csv`
- 输出：`model/loss_curve.png`

`loss_curve.png` 的 loss 子图只展示生成器总损失和实际乘权重后进入生成器 loss 的三项：

- `G Loss Total`
- `G Adv Weighted`
- `Surrogate Weighted`
- `Thickness Weighted`

`D Loss` 单独放在 critic diagnostics 子图中，不再和生成器各项损失画在同一个 loss 子图里。
