# 气动代理模型 train-only 归一化技术路线

## 已确认目标

气动代理模型的归一化统计量只允许从训练集计算，再应用到 train/val/test。需要覆盖：

1. 翼型坐标 `x/y` 的 min/max。
2. 条件输入 `alpha/Re` 的 mean/std。
3. 目标输出 `[CM, CL, CD]` 的 mean/std。

## 执行路线

1. 在 `train_surrogate.py` 中新增数据划分索引构建函数，使用 `surrogate_split_ratio` 和 `surrogate_seed` 生成 train/val/test indices。
2. `AirfoilSurrogateDataset` 构造时接收完整 config，内部先生成并保存 split indices。
3. `AirfoilSurrogateDataset` 只使用 train indices 对应的原始样本计算坐标、条件和目标归一化统计量。
4. 所有样本仍然用 train-only 统计量完成归一化。
5. `split_dataset` 不再重新随机划分，而是复用 dataset 内部的 split indices，保持单一来源。
6. `surrogate_norm.pt` 保存 train-only 统计量。
7. 同步更新 `eval_surrogate.py` 的 dataset 构造方式，保证评估和训练一致。
8. 运行语法检查和短 epoch 冒烟训练，确认流程可执行。

## 注意事项

旧 checkpoint 使用旧的全量数据归一化统计训练，改造后需要重新训练代理模型，再比较验证集和测试集指标。
